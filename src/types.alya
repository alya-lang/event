# Types and data structures for event
# Part of the Alya Language package ecosystem (https://github.com/alya-lang)

struct EventConfig
    name
    prefix
    count
end

function make_config(name = "World", count = 1)
    return EventConfig {
        name: name,
        prefix: "Hello",
        count: count
    }
end
